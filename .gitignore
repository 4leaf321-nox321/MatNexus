# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Node
node_modules/
frontend/dist/
.vite/

# 운영 데이터 — 배포 패키지에도 git에도 넣지 않는다 (deploy.ps1이 이전 폴더에서 이관)
backend/.env
backend/filestore/
backend/logs/

# 배포 산출물 — **앞의 슬래시가 중요하다.**
# `deploy/` 라고만 쓰면 어느 깊이의 deploy 폴더든 걸려서 `scripts/deploy/` 의
# 배포 스크립트 5개가 통째로 빠진다(실측: 릴리스 패키지에 venv_sync.ps1 이 없어
# 설치가 실패했다).
/deploy/
*.zip

# 로컬 도구
.idea/
.vscode/
*.local
