# 개발 지침

@AGENTS.md

규칙의 정본은 [AGENTS.md](AGENTS.md) 하나다. **여기에 규칙을 적지 않는다** —
Codex 를 쓰는 사람은 이 파일을 안 읽고, 두 벌로 두면 한쪽만 고쳐진 채 사람마다
다른 규칙을 따르게 된다.

위 `@AGENTS.md` 가 그 파일을 함께 읽어 온다. 혹시 그 줄이 동작하지 않는 환경이면
**[AGENTS.md](AGENTS.md) 를 직접 열어 읽고 시작한다.**

`tests/architecture/test_agent_docs.py` 가 이 파일이 규칙을 베껴 두지 않았는지
검사한다.
