"""**절대값이 아니라 폭인 물성** — 단위는 같은데 뜻이 다르다.

## 무엇이 문제인가

`mechanical.wlf_c2` 는 단위가 `K` 다. 그런데 그 값은 온도가 아니라 **온도 폭**이다
(WLF 식의 C₂ 는 대개 50~100 K 인 기준 온도로부터의 폭). 유리전이온도 350 K 와
WLF C₂ 51.6 K 가 같은 표에 같은 단위로 앉아 있고, 숫자만 봐서는 구별이 안 된다.

**환산에서 갈린다.** 절대온도는 섭씨로 바꿀 때 영점을 빼지만(350 K → 76.85 °C),
폭은 크기가 그대로다(51.6 K → 51.6 °C). 지금 화면은 문헌 물성을 저장 단위 그대로
보여 주므로 아무 일도 안 일어나지만, 언젠가 「℃ 로 보기」 를 붙이는 날 C₂ 가
**-221.55 °C** 로 뜬다 — 숫자는 그럴듯하고 아무도 안 틀렸다고 못 한다.

이 저장소는 같은 함정을 한 번 밟았다. 값 검색의 「근처」 폭이 그것이다:

    width = abs(units.to_si(near + tolerance, unit) - center)   # 두 점의 차로 환산

## 왜 표에 컬럼을 안 두나

문헌 정의 271종의 정본은 MaterialTwin 원본이고, 적재 스크립트가 배포마다 그것을
다시 읽는다. 우리가 행에 적어 둔 표시는 그때 지워질 수 있다 — 표시의 정본은 코드다.

## 늘릴 때

단위에 영점이 있는 물성(온도)만 해당한다. `1/K`(선팽창계수)처럼 역수인 것은 이미
차이의 단위라 여기 없어도 된다 — `1/K` 와 `1/°C` 는 크기가 같다.
"""

from __future__ import annotations

#: 값이 **폭·차이**인 물성 키. 단위에 영점이 있는 것만 적는다.
#:
#: 개발 DB 실측(2026-09-24): `K` 를 단위로 쓰는 문헌 정의 12종 중 폭인 것은 이
#: 하나다. 나머지 열하나(유리전이·융점·최대/최소 사용온도·열변형·연화점·큐리온도·
#: 점도 기준온도·교차온도·분해온도)는 전부 절대온도다.
SPAN_KEYS: frozenset[str] = frozenset({"mechanical.wlf_c2"})


def is_span(property_key: str | None) -> bool:
    """이 물성의 값이 폭인가 — 환산할 때 **영점을 빼면 안 되는 값**인가."""
    return bool(property_key) and property_key in SPAN_KEYS
