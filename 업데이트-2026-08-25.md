# MatNexus 업데이트 — 2026-08-24 21:00 이후

범위: `a5f1662` ~ `d8e2974` (**v1.59.0 → v1.74.0**, 커밋 20개)
직전 커밋은 8/24 18:22 이므로 21:00 경계는 깔끔하게 갈린다.

    파일        114개 변경 (새 파일 32개)
    줄          +8,485 / −623
    마이그레이션  5개
    새 ADR      1개 (0016)
    릴리스      20번 (v1.59.0 · v1.60.0 · v1.60.1 · … · v1.74.0)

---

## 한눈에

이 밤에 한 일은 크게 셋이다.

| | 무엇을 | 버전 |
|---|---|---|
| ① | **화면을 좁은 데서 꺼냈다** — 옆패널·본문 폭·모달 폭 | v1.59.0 ~ v1.69.0 |
| ② | **견주기 계산을 문헌 데이터로 검증했다** | v1.70.0 |
| ③ | **시험이 주지 않는 물성을 넣는 길을 냈다** — 문헌값·밀시트 | v1.71.0 ~ v1.73.0 |
| ④ | 머리글에 서버 버전 | v1.74.0 |

가장 무거운 것은 ③이다(3개 릴리스, +5,200줄). ①은 개수가 많지만 대부분 배치
문제였다.

---

## ① 화면 — 좁은 데서 꺼냈다 (v1.59.0 ~ v1.69.0)

### 시험 등록에서 재료·시료·시편을 바로 만든다 — v1.59.0

전에는 시험을 올리려면 **재료 화면으로 가서 시료를 먼저 만들고** 돌아와야 했다.
새 시험은 대개 새 시료라 그 왕복이 매번 생겼다. 이제 등록창 안에서 없으면
그 자리에서 만든다.

### 본문 폭을 한 곳에서 정한다 — v1.60.0 · v1.60.1

화면마다 `max-w-*` 를 따로 들고 있어 폭이 제각각이었다. `AppShell` 한 곳으로
모으고 **최대 1600px** 로 넓혔다. 좁게 두는 게 맞는 화면은 시험이 이름으로
들고 있게 했다(`NARROW_BY_DESIGN`).

처리 결과 카드는 열을 늘려 아래 빈 공간을 줄였다.

### 재료 조회 — 옆패널 (v1.61.0 · v1.62.0)

재료 하나를 열면 **뒤로 가기밖에 길이 없었다.** 좌측 사이드바 옆에 재료 목록을
붙여 옆 재료로 바로 건너뛴다. Family·Category 필터가 달렸고, **Category 는
Family 종속**이다(`Metal + PP` 는 늘 0건이다).

검색은 서버가 한다 — 앞 50개만 받아 화면에서 거르면 뒤엣것이 없는 재료가 된다.

### 모달 폭을 폼에 맞춘다 — v1.63.0

시스템 전체 모달을 훑어 기본 폭을 내용에 맞게 키웠다.

### 기준정보 — 축을 세로 목록으로, 「강종」→ Grade (v1.64.0)

축이 가로로 나열돼 있던 것을 옆패널 세로 목록으로 바꿨다.

**「강종」은 틀린 이름이었다.** 이 축은 금속만 담는 자리가 아니다(고무·폴리머도
등급이 있다). `Grade` 로 바로잡았다 — 마이그레이션이 함께 간다.

### 파일 형식·레시피를 시험 종류로 거른다 — v1.65.0 · v1.66.0 · v1.66.1

옆패널에 시험 종류 목록을 두고 「전체 / 고른 종류」로 좁힌다. 레시피는 **부서로도
나눌 수 있는데 켜고 끌 수 있다**(기본 꺼짐, 선택은 기억된다). 그 스위치는 맨
위에 있다.

### 홈에 전체 현황 요약 — v1.67.0

홈이 빈 화면이었다. 재료·시료·시편·시험 수, 시험 종류별 분포, 카드 상태,
**손이 필요한 것**(읽기 실패 · 처리 대기 · 확정 대기)을 요약한다.

**0을 감추지 않는다** — 「읽기 실패 0」이 안 보이면 그 항목이 있는지조차 모른다.

### 시험 종류를 한 번에 하나만 본다 — v1.68.0 · v1.68.1 · v1.68.2

종류마다 카드를 세로로 쌓던 것을 옆패널 목록으로 바꿨다(개발 DB 만 해도 종류
넷에 **채널이 스물셋**이다). 편집창을 넓히고, 채널 줄에서 「차원」 드롭다운이
잘리던 것을 고쳤다.

### 「감사 기록」 → 「변경 이력」 — v1.69.0

실무에서 안 쓰는 말이었다.

---

## ② 견주기 계산을 검증했다 (v1.70.0)

**분포 견주기(AICc + Anderson-Darling)가 실제로 판정하는지 확인했다.**

확인 방법이 요점이다 — **판정 계산을 통째로 지워 봤다.** 시험 18건이 전부
통과했다. 우리가 만든 표본으로만 시험하고 있었기 때문이다(정규분포 표본을
넣고 정규분포가 1등인지 보는 식이라, 계산이 없어도 순서가 맞았다).

문헌 데이터(Treloar 고무 단축 인장 · 신뢰성 벤치마크)를 픽스처로 넣고 다시
시험하니 그제야 잡힌다.

---

## ③ 시험이 주지 않는 물성 (v1.71.0 ~ v1.73.0) — ADR 0016

### 문제

    밀도          ✔  재료 컬럼
    푸아송비       ✔  재료 컬럼
    탄성계수       ✗  처리 결과에서만 온다
    열팽창·비열·열전도  ✗  자리가 아예 없다

**시험을 안 한 재료(대부분이다)로는 해석용 카드를 만들 수 없었다.** 열해석은
통째로 막혀 있었다.

조사해 보니 출처가 둘로 갈린다(EN 10204 3.1).

    문헌·규격   Grade 가 같으면 같다   E · ν · α · Cp · k   → 재료
    밀시트      로트마다 다르다        항복강도 · 인장강도    → 시료

**밀시트는 「이 로트가 규격에 맞나」를 증명하는 문서지 물리 상수표가 아니다.**

### 1단계 — 넣을 자리 (v1.71.0)

재료에 `declared_properties`(JSONB 목록). 넣을 수 있는 항목은 **기준정보의 새 축**
`물성 항목` 이 정한다(D7) — 열해석을 안 하는 부서에 비열 칸이 뜰 이유가 없고,
코드에 박으면 항목 하나 넣으려고 배포를 기다려야 한다.

한 줄의 모양: `항목 · SI 값 · 적은 단위 · 출처 · 근거 문서 · 잰 온도`

- **출처가 필수다.** 값만 있고 어디서 왔는지 모르면 그 값으로 돌린 해석의 근거를
  되짚을 수 없다. 「모름」을 안 둔다 — 두면 대부분이 거기로 간다. 대신 「추정」을
  둔다(추정도 출처다)
- **근거 문서까지 받는다.** 「문헌」만으로는 어느 핸드북 몇 판인지 알 수 없다
- **차원 검사가 붙는다.** 비열 자리에 `W/(m.K)` 를 넣으면 거절한다 — ADR 0013 이
  남겨 둔 구멍이 이 축에서는 막혔다
- **한 항목은 한 줄.** 두 줄이면 카드가 어느 것을 쓸지 못 정하고, 그 판단을
  입력에서 안 하면 나중에 조용히 하나가 이긴다

### 2단계 — 덱까지 (v1.72.0)

1단계는 넣을 자리만 만들었다. 그 상태로 두면 **넣어 두고 안 쓰는 기능**이다.

- **측정 → 선언 순으로 떨어진다.** 시험을 한 재료는 잰 값, 안 한 재료는 문헌값.
  출처가 `measured` 대신 `declared:literature` 가 되고 **근거 문서를 카드에
  복사한다**(참조로 두면 재료를 고치는 순간 확정한 카드의 근거가 달라진다)
- **열물성 블록**(`thermal`)을 새로 만들었다. `*ELASTIC` 은 두 값을 한 줄에
  받지만 열물성은 키워드가 셋으로 갈리고 **하나만 있어도 낼 수 있다**

```
*EXPANSION, TYPE=ISO      ← 새로
*SPECIFIC HEAT            ← 새로
*CONDUCTIVITY, TYPE=ISO   ← 새로
```

- **`ZERO=` 는 기준 온도가 있을 때만 붙인다.** 없는데 293.15 를 적어 넣으면
  **덱은 멀쩡히 돌고 열응력만 통째로 어긋난다**

### 3단계 — 시험 없이도, 그리고 밀시트 (v1.73.0)

2단계 마무리에 *"시험을 한 번도 안 한 재료로 덱이 나간다"* 고 적었는데
**반만 맞았다.** 카드를 만드는 길이 대표 곡선 하나뿐이고 `test_type_id` 가
NOT NULL 이라, 시험이 0건인 재료는 선언 물성을 채워도 카드가 안 만들어졌다
(개발 DB 재료 94개 중 14개).

- `POST /fitting/cards/declared` — 시험종류·방향을 **비운다.** 자리표시를 넣으면
  그 카드가 인장시험에서 나온 것처럼 보이고, 덱을 받은 사람이 잰 값으로 읽는다
- **빈 카드를 안 만든다.** 값이 없는 카드는 목록에서 「이 재료는 물성이 있다」고
  말하는 거짓말이 된다. 화면도 누르기 전에 알고, **그 답을 카드를 만드는 함수가
  낸다**(화면이 따로 판정하면 "실린다" 고 한 값이 안 실린다)
- **밀시트는 시료에 붙는다.** 항목 축에 「붙는 곳」(재료 \| 시료)을 더했다. 층이
  어긋나면 **어디에 적어야 하는지 말한다**
- **이 층의 쓸모는 대조다.** 같은 물성을 우리 처리 결과가 낸다
  (`proof_stress`·`tensile_strength`). 밀시트 값과 잰 값을 나란히 놓는다 —
  **판정은 안 한다.** 몇 %부터 문제인지는 규격과 용도가 정하고, 상수로 박으면
  그 숫자가 규격 행세를 한다

**일부러 안 이은 것 둘**

- **연신율** — 밀시트의 A 는 파단 후 연신율, 우리 `elongation_observed` 는 시험 창
  안의 관측 최대 변형률. 가깝지만 같지 않다
- **경도** — `HV 200`·`HB 200`·`HRC 200` 은 환산식이 없다. 한 칸에 받으면 숫자는
  그럴듯한데 뜻이 다른 값이 저장된다. 시험 척도를 값의 속성으로 드는 것이 먼저다

---

## ④ 머리글에 서버 버전 (v1.74.0)

사이드바 머리글이 **지금 도는 서버의** 버전을 말한다(`GET /api/health`).

번들에 안 박은 이유: 그것은 *빌드된* 버전이지 *지금 도는* 서버가 아니다. 배포가
반쯤 끝났거나 서비스가 안 내려갔다 올라온 상태에서 둘이 갈리고, 그때 화면이
거짓말을 한다. **답이 틀린 것은 못 답하는 것보다 나쁘다.**

---

## 데이터 구조 변경 (마이그레이션 5개)

| 리비전 | 무엇 |
|---|---|
| `b3cecec7f07c` | `물성 항목` 축 |
| `02306966487a` | `materials.declared_properties` |
| `59411592daec` | `강종` 축 → `Grade` 로 이름 바로잡기 |
| `198f219ea68e` | `property_cards.test_type_id`·`orientation` 을 비울 수 있게 |
| `9efc6878b742` | `samples.declared_properties` |

`198f219ea68e` 의 되돌리기는 **선언 카드가 있으면 멈춘다.** 조용히 지우면
그 값으로 해석이 돌았을 수 있다는 흔적까지 사라진다.

---

## 실측으로 드러난 것 (개발계획 §10.5 에 추가)

1. **"덱이 나간다" 고 적었는데 나갈 길이 없었다** (v1.73.0 에 고침)
   구현한 것과 그것이 닿는 범위를 구별하지 않았다. **새 경로를 만들면 그 경로가
   없던 데이터로 한 번 태운다.**

2. **안 열리는 목록에 대고 「없다」를 검사하면 아무것도 검사하지 않은 것이다** (v1.72.0)
   Radix Select 가 jsdom 에서 한 번도 열리지 않아, 「비열 자리에 W/(m.K) 가 안
   뜬다」가 목록을 열어 보지도 않고 통과했다. 차원 필터를 통째로 없애도 통과했다.
   **부정 단언은 대상이 살아 있다는 증거가 같은 시험 안에 있어야 한다.**

3. **아키텍처 시험이 설계 실수를 두 개 잡았다** (v1.71.0)
   모듈 간 import 와 프론트 단위표 누락. 예외로 적는 대신 배치를 고쳤다.

4. **판정하는 계산을 지워도 시험이 통과했다** (v1.70.0)
   자기가 만든 표본으로는 견주기를 시험할 수 없다.

---

## 배포할 때

```powershell
# 마이그레이션 5개가 함께 간다
.\deploy.ps1 -AppPath 'C:\Server\MatNexus' -ZipPath .\deploy_package.zip
```

- **기준정보 시드가 돈다** — `물성 항목` 축에 기본 8개(탄성계수·전단탄성계수·
  열팽창계수·비열·열전도도 / 항복강도·인장강도·연신율)가 생긴다. **이미 있는 값은
  안 건드린다**
- 「강종」 축 이름이 `Grade` 로 바뀐다
- 배포 뒤 머리글에 `v1.74.0` 이 뜬다

---

## 아직 안 한 것

- **온도 의존 표** — 지금은 값 하나에 온도 하나다. E·항복강도는 온도를 크게 탄다
  (`temperature_k` 를 지금 받아 두어 옛 값은 살아남는다)
- **경도** — 시험 척도(HV·HB·HRC)를 값의 속성으로 들어야 한다. 단위가 아니다
- **OpenRadioss 열물성** — `/HEAT/MAT` 는 재료 카드가 따로라 별도 렌더러가 필요
- **워크벤치** — 계획의 중심 화면이 아직 stub 이다
- **번들** — 카드가 여러 장 쌓인 뒤에 만든다(현재 11장)

### 쓰이기를 기다리는 것

개발 DB 기준으로 **v1.71~73 이 만든 칸은 아직 비어 있다.**

    선언 물성을 적은 재료   0 / 94
    밀시트 값을 적은 시료   0 / 92
    파싱됐지만 처리 안 한 시험   73 / 125

일괄 처리는 이미 있고(채택까지 한 번에), 홈이 「처리 대기 73」을 띄우고 있다 —
**코드가 막고 있는 것이 아니다.**
