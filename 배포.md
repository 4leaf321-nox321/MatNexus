# 배포

운영 서버에 MatNexus 를 올리는 절차. **초기 배포(Day 0)** 와 **업데이트 배포**는
쓰는 스크립트가 다르다.

판단 근거는 [ADR 0003](docs/adr/0003-윈도우-배포-방식.md) 에 있다. 여기는 절차만 적는다.

| 상황 | 스크립트 |
|---|---|
| 서버에 처음 올린다 (DB도 없다) | `precheck.ps1` → **`install.ps1`** |
| 이미 돌고 있는 서버를 갱신한다 | `backup.ps1` → **`deploy.ps1`** |
| 갱신했는데 문제가 생겼다 | **`rollback.ps1`** |

릴리스 자산은 **`deploy_package.zip` 하나**다. 코드·SPA·wheel 번들·배포 스크립트가
전부 그 안에 있다. 폐쇄망이면 이 파일만 반입하면 된다.

---

## 0. 사람이 미리 준비할 것

스크립트가 **해 주지 않는 것**은 둘뿐이다.

| | 확인 |
|---|---|
| **PostgreSQL 17** 설치·기동 | `pg_isready` 가 응답하고, 관리자(`postgres`) 비밀번호를 안다 |
| **Python 3.12** 설치 | `py -0p` 목록에 3.12 가 있다 (wheel ABI 가 빌드와 맞아야 한다) |

나머지 — 데이터베이스 생성, 스키마, 관리자 계정, 방화벽, 실행 스크립트 — 는
`install.ps1` 이 만든다.

디스크는 **5GB 이상** 비어 있어야 한다(앱 2벌 + 가상환경 + 시험 데이터). 시험
데이터는 GB 단위로 자라므로 실제로는 더 넉넉히 잡는다.

PowerShell 은 **관리자 권한**으로 연다. `C:\Server` 에 폴더를 만들고 방화벽 규칙을
추가하기 때문이다.

---

## 1. 초기 배포 (Day 0)

### 1-1. 배포 스크립트 꺼내기

스크립트를 **`C:\Server\tools\MatNexus\`** 하위에 둔다. `C:\Server\tools` 바로
아래에 두면 같은 서버에 사는 다른 앱의 `deploy.ps1`·`rollback.ps1` 과 이름이 겹쳐
서로를 덮어쓴다 — 실제로 이 PC 의 `C:\Server\tools` 에 다른 프로젝트의 같은 이름
스크립트가 이미 있었다.

```powershell
# 인터넷이 되는 경우
gh release download --repo 4leaf321-nox321/MatNexus --pattern deploy_package.zip
Expand-Archive .\deploy_package.zip .\unpacked -Force

New-Item -ItemType Directory -Force -Path 'C:\Server\tools\MatNexus'
Copy-Item .\unpacked\install.ps1, .\unpacked\precheck.ps1, `
          .\unpacked\deploy.ps1, .\unpacked\rollback.ps1, .\unpacked\backup.ps1 `
          'C:\Server\tools\MatNexus\' -Force
```

폐쇄망이면 `deploy_package.zip` 을 USB 등으로 반입해 같은 방식으로 푼다.

### 1-2. 환경 점검

```powershell
cd C:\Server\tools\MatNexus
.\precheck.ps1 -AppPath 'C:\Server\MatNexus' `
               -DatabaseUrl 'postgresql+psycopg://postgres:<암호>@localhost:5432/matnexus'
```

막는 것: Python 3.12 없음 · 디스크 부족 · 폴더 쓰기 권한 없음 · 포트 8010 사용 중 ·
PostgreSQL 도달 불가. **여기서 걸리는 것을 먼저 해결한다** — 설치 도중에 발견하면
반쯤 만들어진 상태가 남는다.

### 1-3. 설치

```powershell
.\install.ps1 -AppPath 'C:\Server\MatNexus' -DbPassword '<postgres 암호>'

# 폐쇄망이면 반입한 zip 을 직접 지정
.\install.ps1 -AppPath 'C:\Server\MatNexus' -DbPassword '<암호>' `
              -ZipPath 'C:\tmp\deploy_package.zip'
```

바꿀 수 있는 것(기본값): `-DbHost localhost` · `-DbPort 5432` · `-DbName matnexus` ·
`-DbUser postgres` · `-Port 8010` · `-AdminEmail admin@matnexus.local` ·
`-PythonExe`(3.12 를 자동으로 못 찾을 때) · `-Tag v0.1.6`(특정 버전).

install 이 하는 일:

```
1. 코드 배치            zip 을 풀어 <AppPath> 로
2. 폴더 생성            <AppPath>_venvs, <AppPath>_data (filestore·logs)
3. 가상환경             wheel 번들에서 설치 — 인터넷 없이도 된다
4. backend\.env 생성    이미 있으면 건드리지 않는다
5. 데이터베이스 생성    CREATE DATABASE ... ENCODING 'UTF8' (이미 있으면 넘어간다)
6. alembic upgrade head 스키마 전체
7. 설치 시드            기본 부서 · 시험 종류 · 관리자 계정
8. 방화벽               TCP 8010 인바운드 허용
9. run_server.ps1 / run_worker.ps1 생성
```

**각 단계는 멱등하다.** 중간에 끊겨 다시 실행해도 같은 결과가 나오고, 특히 이미
있는 관리자 계정의 비밀번호를 되돌리지 않는다.

마지막에 **관리자 비밀번호가 한 번만 출력된다.** 다시 표시되지 않으니 그 자리에서
받아 적는다. 첫 로그인 시 변경이 강제된다.

### 1-4. 기동

**창 두 개로 돈다.** 워커가 없으면 알림과 비동기 작업이 큐에 쌓이기만 한다.

```powershell
cd C:\Server\MatNexus ; .\run_server.ps1     # 창 1 — API·화면
cd C:\Server\MatNexus ; .\run_worker.ps1     # 창 2 — 파싱·알림
```

### 1-5. 확인

```powershell
Invoke-WebRequest -UseBasicParsing http://127.0.0.1:8010/api/health
```

그다음 브라우저에서 `http://<서버주소>:8010/` 로 들어가:

1. 관리자로 로그인 → 비밀번호 변경 화면이 뜬다 → 바꾼다
2. **재료 등록** → 시료 추가 → 시편 추가
3. 시편에 `.tra` 파일 하나 올리기 → 상태가 `대기` → `완료` 로 바뀐다
   (안 바뀌면 **워커 창이 떠 있는지** 본다)
4. 시험을 열어 곡선이 그려지는지

여기까지 되면 등록·워커·저장소·화면이 전부 통한 것이다.

---

## 2. 업데이트 배포

### 2-1. 앱을 먼저 중지한다

`run_server.ps1` 창과 `run_worker.ps1` 창을 **둘 다** 닫는다. 윈도우는 실행 중인
파일을 잠그므로, 켜 둔 채 배포하면 `deploy.ps1` 이 **거부한다**(부분 이동으로
망가지는 것을 막기 위해 일부러 그렇게 만들었다).

### 2-2. 백업

```powershell
cd C:\Server\tools\MatNexus
.\backup.ps1 -AppPath 'C:\Server\MatNexus' -BackupRoot 'D:\backup\matnexus' -KeepDays 30
```

**DB 와 파일스토어를 같은 시각에 함께 받는다.** DB 에는 곡선의 경로와 해시가,
파일스토어에는 그 내용이 있어서 한쪽만 되돌리면 "DB 에는 있는데 파일이 없는" 행이
생긴다. 받은 폴더의 `MANIFEST.txt` 에 복구 절차가 적혀 있다.

> **한 번은 실제로 복구해 보라.** 받아만 두고 복구해 본 적 없는 백업은 백업이 아니다.

### 2-3. 배포 스크립트 갱신

스크립트 자체가 바뀌었을 수 있으므로 새 릴리스에서 다시 꺼내 덮는다(1-1 과 같음).

### 2-4. 배포

```powershell
cd C:\Server\tools\MatNexus
.\deploy.ps1 -AppPath 'C:\Server\MatNexus'

# 폐쇄망
.\deploy.ps1 -AppPath 'C:\Server\MatNexus' -ZipPath 'C:\tmp\deploy_package.zip'
```

deploy 가 하는 일:

```
1. 실행 중이면 거부
2. 현재 설치를 <AppPath>_prev 로 이동      ← 롤백용
3. 새 코드를 <AppPath> 로
4. backend\.env 를 _prev 에서 이어받는다
5. requirements 가 바뀌었으면 가상환경 재생성
6. alembic upgrade head
```

**`<AppPath>_data`(filestore·logs)는 건드리지 않는다.** 운영 데이터가 앱 폴더
바깥에 있어서 배포마다 복사할 것이 없다.

### 2-5. 기동과 확인

1-4, 1-5 와 같다. 갱신 뒤에는 **새로 들어간 기능 하나를 실제로 눌러 본다.**

### 2-6. 문제가 생기면

```powershell
.\rollback.ps1 -AppPath 'C:\Server\MatNexus'
```

`_prev` 에 있던 직전 버전을 되돌린다. **스키마는 되돌아가지 않는다** — 마이그레이션은
앞으로만 간다. 스키마까지 되돌려야 하면 2-2 의 백업에서 DB 와 파일스토어를 함께
복구한다.

---

## 3. 폴더 배치

```
C:\Server\MatNexus          코드 — 배포마다 통째로 교체된다
C:\Server\MatNexus_prev     직전 버전 (롤백용)
C:\Server\MatNexus_venvs    가상환경 (requirements 가 바뀔 때만 재생성)
C:\Server\MatNexus_data     filestore · logs  ← 배포가 건드리지 않는다. 백업 대상
C:\Server\tools\MatNexus    배포 스크립트 (앱 폴더 바깥, 프로젝트별로 분리)
```

운영 데이터를 앱 폴더 **바깥**에 두는 이유: CAE 시험 데이터는 GB 단위라, 배포마다
복사해 옮기는 방식이 성립하지 않는다.

---

## 4. 관리자 계정

로그인 아이디는 **이메일 형식이 아니어도 된다**(`admin` 같은 짧은 아이디 허용).

```powershell
cd C:\Server\MatNexus\backend
$py = 'C:\Server\MatNexus_venvs\backend\Scripts\python.exe'

& $py scripts\set_admin.py --email admin --password '...'                     # 비밀번호 재설정
& $py scripts\set_admin.py --email admin --password '...' --rename-from old@x  # 아이디 변경
& $py scripts\set_admin.py --email admin --password '...' --no-force-change    # 강제 변경 끄기
```

`--no-force-change` 는 파일럿·개발 편의용이다. **운영에서는 강제 변경을 켜 둔다** —
시드 비밀번호가 그대로 남는 것이 폐쇄망 설치에서 가장 흔한 사고다.

### 다른 앱에서 쓰던 아이디·비밀번호로 맞추기

같은 서버의 다른 앱과 같은 계정으로 로그인하고 싶을 때가 있다. **계정이 공유되는
것은 아니다** — MatNexus 사용자는 자기 DB(`matnexus.users`)에만 있고 SSO 도 없다.
같은 값으로 **새로 만드는** 것이다.

```powershell
# 1. 설치할 때 아이디를 맞춘다
.\install.ps1 -AppPath 'C:\Server\MatNexus' -DbPassword '<DB 암호>' -AdminEmail 'admin'

# 2. 설치가 끝나면 비밀번호를 맞춘다
cd C:\Server\MatNexus\backend
$py = 'C:\Server\MatNexus_venvs\backend\Scripts\python.exe'

$pw = Read-Host '비밀번호' -AsSecureString
$plain = [Runtime.InteropServices.Marshal]::PtrToStringAuto(
             [Runtime.InteropServices.Marshal]::SecureStringToBSTR($pw))
& $py scripts\set_admin.py --email admin --password $plain --no-force-change
```

1단계에서 출력되는 **난수 비밀번호는 2단계에서 덮어쓰므로 받아 적지 않아도 된다.**

`--no-force-change` 를 붙이는 이유: 이미 쓰던 비밀번호라면 첫 로그인에 또 바꾸라고
할 이유가 없다. 붙이지 않으면 로그인하자마자 변경 화면으로 간다.

`Read-Host` 로 받는 이유: 비밀번호를 명령줄에 그대로 치면 **PowerShell 기록 파일에
남는다**(`$env:APPDATA\Microsoft\Windows\PowerShell\PSReadLine\ConsoleHost_history.txt`).

> **앱 간 비밀번호 공유는 한쪽이 털리면 같이 털린다.** 운영 편의가 그보다 크다고
> 판단해서 그렇게 하는 것이면 괜찮다 — 모르고 그렇게 되는 것과는 다르다.

---

## 5. 자주 걸리는 것들

**배포가 "실행 중" 이라며 거부한다.** 서버 창 하나만 닫고 워커 창을 잊는 경우가
많다. 둘 다 닫는다.

**`.ps1` 이 한글 깨짐·구문 오류.** Windows PowerShell 5.1 이 BOM 없는 파일을 CP949 로
읽는다. `.ps1` 은 **UTF-8 BOM** 으로 저장해야 한다. 편집기가 BOM 을 떼어냈다면:

```powershell
$bom = New-Object System.Text.UTF8Encoding $true
$text = [System.IO.File]::ReadAllText($path, (New-Object System.Text.UTF8Encoding $false))
[System.IO.File]::WriteAllText($path, $text, $bom)
```

**시험 상태가 `대기` 에서 안 움직인다.** 워커 창이 없다. 파싱은 워커가 한다.

**`gh` 가 없다 / 비공개 저장소라 못 받는다.** `-ZipPath` 로 반입한 zip 을 직접
지정한다. 두 스크립트 모두 `-ZipPath` 를 받는다.

**Python 3.12 를 못 찾는다.** `py -0p` 로 경로를 확인하고 `-PythonExe` 로 지정한다.
3.12 여야 하는 이유는 wheel 번들이 그 ABI 로 빌드되기 때문이다.

**마이그레이션이 오래 걸린다.** v0.1.4 → v0.1.6 처럼 여러 단계를 건너뛰면 한 번에
16건이 돈다. 정상이다. 빈 DB 에 19건을 연속 적용하고 `alembic check` 로 모델과
일치함을 확인해 두었다(개발계획 §0).
