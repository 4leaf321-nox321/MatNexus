# MatNexus 업데이트 — 2026-08-25 ~ 08-26

범위: `d8e2974` ~ `4f157d5` (**v1.74.0 → v1.102.0**, 커밋 33개)
앞 문서([업데이트-2026-08-25.md](업데이트-2026-08-25.md))가 끝난 지점부터다.

    파일        127개 변경 (새 파일 38개)
    줄          +17,268 / −1,013
    마이그레이션  4개
    릴리스      32번 (v1.75.0 · … · v1.102.0)

---

## 한눈에

| | 무엇을 | 버전 |
|---|---|---|
| ① | **여럿을 한꺼번에** — 등록·삭제·수정을 표와 목록에서 | v1.85.0 ~ v1.91.0 |
| ② | **단위와 용도** — 화면을 CAE 단위계로, 용도는 여러 개로 | v1.88.0 · v1.89.0 |
| ③ | **대표 곡선을 믿을 수 있게** — 원곡선을 깔고, 쓸 시험을 고른다 | v1.93.0 ~ v1.97.0 |
| ④ | **읽기 실패를 사람이 풀 수 있게** | v1.98.0 ~ v1.101.0 |
| ⑤ | **셋이 함께 고칠 세팅** | v1.102.0 |
| ⑥ | 카드 목록 · 열물성 · 복구 리허설 (앞 문서 흐름의 마무리) | v1.75.0 ~ v1.84.0 |

가장 무거운 것은 ①(7개 릴리스)과 ③이다. **③이 이번 범위에서 가장 값이 큰
변화**다 — 대표 곡선이 적절한지 판단할 근거가 화면에 없었다.

---

## ① 여럿을 한꺼번에 (v1.85.0 ~ v1.91.0)

### 무엇이 됐나

| | 전 | 후 |
|---|---|---|
| 시험 삭제 | 한 건씩, 확인 없이 | 체크박스로 여러 건, 확인창 |
| 시험 수정 | 없음 | 골라서 칸 하나를 한 번에 |
| 재료 등록 | 창을 열고 닫기 반복 | **표에 붙여 넣기** |
| 재료 삭제 | 한 건씩 | 체크박스로 여러 건 |
| 시료 삭제 | 없음 | 재료 안에서 |

### 재료 등록이 표가 됐다 — v1.86.0 · v1.87.0

처음엔 텍스트 상자에 `Grade, 상세, 두께` 순으로 적게 했다. **무엇을 어느 자리에
적어야 하는지가 화면에 없었다** — 순서를 외워서 적어야 했고, 재료의 칸이 여덟인데
셋만 받은 것도 그래서다.

표로 바꾸니 셋이 한꺼번에 없어졌다. 엑셀에서 복사해 그대로 붙여 넣을 수 있고,
**재료 칸이 빈 줄은 위 줄의 재료에 붙는다** — 한 재료 아래 시료 여럿, 한 시료
아래 시편 여럿이 표에서 그대로 읽힌다.

```
Metal Steel SECC MDOI 1.0 │ LOT-A │ MD 1
                          │       │ MD 2
                          │ LOT-B │ TD 1
```

### 되는 것은 하고, 안 된 것은 이름을 말한다

한 건이 막혔다고 나머지를 되돌리지 않는다. 스무 건을 골라 지웠는데 「실패」 한
줄만 돌아오면 어느 것이 문제인지 모른 채 하나씩 다시 눌러야 한다.

서버가 **마디마다 세이브포인트**를 두고, 막힌 것은 줄 번호와 이유로 돌려준다.

### 사업부 — v1.90.0 · v1.91.0

시험에 「사업부」가 붙었다. **부서(workspace)와 다른 축이다** — 부서는 누가 볼 수
있는가를 정하고, 사업부는 누가 낸 데이터인가를 적는다. 한 부서 계정으로 여러
사업부의 판을 올리는 일이 있고, 그때 부서로는 둘을 못 가른다.

목록은 기준정보가 관리한다(정책 `open` — 부서가 스스로 늘린다).

### 고른 시험을 한꺼번에 고친다 — v1.91.0

고칠 수 있는 칸은 다섯이다(사업부·장비·시험자·시험일·메모). **나머지는 일부러
안 연다** — 시편·재료·종류는 이름을 만드는 값이고, 상태·채택은 처리 흐름이 쓰는
값이라 손으로 옮기면 「읽힌 적 없는데 처리됨」 같은 상태가 생긴다.

---

## ② 단위와 용도 (v1.88.0 · v1.89.0)

### 화면 단위를 CAE 단위계로 — v1.88.0

| | 화면 | 저장 |
|---|---|---|
| 밀도 | **tonne/mm³** | kg/m³ |
| 변형률 | **무차원** | 1 |
| 길이·힘·응력·속도·온도 | mm · N · MPa · mm/min · °C | m · N · Pa · m/s · K |

**저장은 한 자리도 안 바뀌었다.** 저장까지 옮기면 스칼라 컬럼 7개뿐 아니라
`processing_results.scalars` 전부와 곡선 Parquet 420개를 다시 써야 하고, 그러면
**모든 sha256 이 바뀐다** — 그 해시가 재현 사슬의 앵커다.

그 과정에서 잡힌 것: 밀도 단위가 화면 여섯 곳에 손으로 박혀 있었다. 표만 바꾸면
`kg/m³` 라고 적힌 칸에 tonne/mm³ 값을 받는다 — **10¹² 배 어긋난 값을 사람이 자기
손으로 적어 넣는 구조**였다.

### 용도는 여러 개다 — v1.89.0

적용 제품·부위가 칸 하나였다. 한 재료가 여러 제품에 들어가는데 자리가 없어서,
쓰는 쪽은 `도어이너/후드이너` 처럼 **한 칸에 두 값을 적는다.** 그러면 기준정보가
그 덩어리를 새 용어로 만들고 「도어 이너」로는 검색이 안 된다.

`material_uses` 표로 옮겼다. 값은 마이그레이션이 옮기고, 되돌리기는 용도가 둘
이상인 재료가 있으면 **거절한다.**

---

## ③ 대표 곡선을 믿을 수 있게 (v1.93.0 ~ v1.97.0)

**이번 범위에서 가장 값이 큰 변화다.**

### 문제

적합 화면이 대표 곡선만 그렸다. 그 그림에서는

- 시편 셋이 거의 겹쳐 있어서 평균이 그 자리인 것과
- 하나가 딴 데로 가서 평균이 그쪽으로 끌려간 것이

**똑같이 생겼다.** 대표가 적절한지 판단할 근거가 화면에 없었다.

### 넣은 것 셋

**원곡선을 뒤에 깐다** (v1.94.0 · v1.96.0) — 시편별 곡선이 대표 뒤에 흐리게
깔린다. 물성 탭과 카드 탭 **둘 다**. 처음엔 카드 탭만 고쳤는데, 같은 그림이 두
군데 있으면 한 곳만 고친 것은 안 고친 것과 비슷하다.

**쓸 시험을 고른다** (v1.93.0) — 이상치 둘을 뺀 8건짜리와 10건짜리를 **나란히
두고 견줄 수 있다.** 전에는 시험의 채택을 푸는 수밖에 없었고, 그러면 통계와 다른
카드까지 따라 바뀌었다. 이상치는 여전히 **표시만** 한다 — 빼는 것은 사람이 정한다.

**차트에 확대·이동·전체 보기·여러 값 읽기** (v1.97.0) — 가리킨 x 에서 모든
곡선의 값을 표로 읽어 준다. 여러 곡선을 깔아 놓고 값은 하나만 보여 주면 흩어짐이
보이는데 얼마나 벌어졌는지는 못 읽는다.

> **차트 라이브러리는 다시 재 보고 안 쓰기로 했다.** 파일 머리말이 "Phase 3 에서
> 다시 판단한다" 고 약속해 뒀고, 그 조건이 다 갖춰져서 실제로 쟀다. 점이 여전히
> 300~1200 이라 라이브러리가 값을 하는 지점에 안 닿았다. 다음 문턱도 적어 뒀다 —
> 점이 수만이 되면 uPlot.

### 예시 데이터 — v1.95.0

개발 서버에 시험이 한 종류뿐이라 위 기능들이 도는지 눈으로 볼 표본이 없었다.
**예시 곡선 열 벌**을 만들어 운영과 같은 길로 넣었다(`scripts/demo_curves.py` ·
`demo_load.py`).

공개된 DP590 범위(항복 330~430 · 인장 590~700 MPa)를 목표로 한 **합성 곡선이고,
그렇게 표시했다** — 재료 이름(`DEMO-DP590`)·별칭·메모·파일 머리글까지. 나온 값은
항복 381~420 · 인장 582~668(평균 617, CV 3.7%)로 그 범위 안에 든다.

---

## ④ 읽기 실패를 사람이 풀 수 있게 (v1.98.0 ~ v1.101.0)

이 넷은 **한 사용자의 막힘 하나**에서 연달아 나왔다.

### 표준 인장 처리를 한 번에 채운다 — v1.98.0

새 레시피가 빈 목록에서 시작해서, 사람들은 「공칭 → 정렬 → 강도」 셋에서 멈춘다.
그 구성은 잘 돌아 보이면서 **카드도 대표 곡선도 못 만든다.**

경고는 이미 있었다. 그런데도 같은 질문이 왔다 — **말하는 것과 넣어 주는 것은
다르다.** 어느 자리에·어떤 옵션으로·왜 그 순서인지가 또 하나의 문제였다.
표준 열한 단계를 한 번에 넣는 길을 뒀다.

### 읽을 형식을 골라 다시 읽는다 — v1.99.0

「다시 읽기」가 **같은 자동 선택을 그대로 반복**했다. 자동이 틀리면 고칠 자리가
없었다. 이제 형식을 골라 읽고, 고른 것은 시험에 남는다.

### 무엇이 틀렸는지 말한다 — v1.100.0

문제의 파일을 열어 보니 **인장 `.tra` 가 DMA 종류로 올라가 있었다.** 안내는
「형식 프로파일을 만드세요」였는데, **그 말을 따르면 영영 못 고친다.**

시스템은 알 수 있었다 — 다른 종류의 파서를 훑으면 「이 파일은 인장시험이 읽는
형식」이 나온다. 이제 그렇게 말한다.

### 종류를 바로잡는다 — v1.101.0

**아직 아무것도 안 나온 시험만** 종류를 바꿀 수 있다. 곡선·처리 결과가 있으면
409 로 막는다 — 말없이 지워 주면 그 사람은 무엇이 사라졌는지 모른 채 새 이름을
얻는다.

바꿀 때 회차를 새 종류 안에서 다시 매기고, 새 종류에 없는 조건은 버리고 몇 칸인지
말하고, 옛 종류의 형식 고정을 푼다.

---

## ⑤ 셋이 함께 고칠 세팅 (v1.102.0)

물성 개발자 둘(Claude · Codex)이 붙는다. 정리한 것 하나가 방향을 정했다 —
**막는 힘이 세 층이고 힘의 차이가 크다.**

    기계   CI + 브랜치 보호        막힌다
    사람   CODEOWNERS + 리뷰       대체로 막힌다
    선의   AGENTS.md / CLAUDE.md   안 막힌다

만든 것: `AGENTS.md` 정본화(계획서가 이름만 적어 뒀고 **실제로는 없었다**) ·
경로 소유(`CODEOWNERS`) · CI 경계 검사 · [docs/협업.md](docs/협업.md) ·
아키텍처 시험 2개.

**「스코프 안에서 바로잡는다」 조항에 단서를 붙였다.** 65의 부채를 막으려고 넣은
예외인데, 셋이 함께 고치면 **남의 파일을 「규칙에 맞게」 고치는 것을 정당화한다.**

> 아직 남은 일 둘: `CODEOWNERS` 에 실제 GitHub 아이디, 그리고 **브랜치 보호를
> 웹에서 켜는 것.** 안 켜면 CI 검사가 있어도 그냥 push 로 넘어간다.

---

## ⑥ 앞 문서 흐름의 마무리 (v1.75.0 ~ v1.84.0)

| | 무엇 | 버전 |
|---|---|---|
| 카드 목록 | 재료를 거치지 않고 카드를 찾고 바로 내보낸다 | v1.75.0 |
| 복구 리허설 | 백업이 실제로 복구되는지 재 봤다(94재료·125시험·164결과, sha256 전부 일치) | v1.76.0 |
| 온도 의존 표 | 선언 물성이 온도별 값을 든다 | v1.77.0 |
| 경도 척도 | HV·HB·HRC 는 단위가 아니라 척도다 — 환산하지 않는다 | v1.78.0 |
| 열해석 덱 | OpenRadioss `/HEAT/MAT` | v1.79.0 |
| 전체 훑기 | 스스로 찾은 버그 다섯을 고쳤다 | v1.80.0 |
| 모달·글자·흐름도 | 스크롤·폭·생성 날짜 | v1.81.0 ~ v1.84.0 |

**v1.80.0 의 다섯 중 하나는 내가 사흘 전에 만든 것**이었다 — `*EXPANSION ZERO=`
가 관계없는 물성의 온도를 가져갔다. 나머지 넷도 「전에도 그랬다」로 넘겼던 것들이다.

---

## 마이그레이션 4개

배포할 때 `alembic upgrade head` 가 돈다. 개발 DB 에는 이미 적용했다.

| 리비전 | 무엇 |
|---|---|
| `cc213f8690a2` | 선언 물성이 온도 표를 든다 |
| `a1c4b70e2f18` | 용도가 여러 개 — `material_uses` (**값을 옮긴다**) |
| `c7d21a9f4b60` | 사업부 축 |
| `d3f5a81c62e7` | 읽을 형식 고정 |

`a1c4b70e2f18` 의 되돌리기는 **용도가 둘 이상인 재료가 있으면 거절한다** — 칸이
하나뿐이라 어느 것을 남길지 정할 수 없고, 말없이 하나만 남기면 나머지는 영영
사라진다.

---

## 이 기간에 배운 것 (docs/개발계획.md §10.5)

새로 적은 항목 열둘 중, 다음에 또 걸릴 만한 셋:

**「후보가 하나뿐이면 사보타주가 안 문다」** — 「고른 것이 쓰인다」를 확인하려면
안 골랐을 때와 결과가 달라야 한다. 이 세션에서 **같은 함정을 세 번** 만났다
(형식 고정 · 재채번 · 확대 범위).

**「상태가 바뀌는 것을 확인하는 시험은 화면이 바뀌는 것을 확인하지 않는다」** —
확대 시험을 「단추가 뜨나」로 썼더니, 확대를 아예 안 하게 망가뜨려도 통과했다.

**「안 보낸 것과 비운 것을 구별한다」** — 이 구별이 필요한 자리가 **셋**이었다
(선언 물성 · 일괄 수정 · 읽을 형식). 부분 수정을 받는 API 를 만들 때는 처음부터
이 셋을 정하는 편이 낫다.

---

## 지금 상태와 다음

**막힌 것 없음.** 예시 데이터가 들어가 있어 카드 만들기를 바로 해 볼 수 있다.

다음 후보 셋:

1. **덱을 실제 솔버로 한 번** — 계획서가 Phase 4 수용 기준으로 적어 둔 것.
   형식은 65 골든 픽스처와 대조했지만 진짜 Abaqus/OpenRadioss 로는 안 돌려 봤다.
2. **번들** — 여러 카드 + manifest + checksums 를 결정적 ZIP 으로. Phase 4 남음 ②.
3. **흔들리는 프론트 시험 둘** — 부하가 걸린 상태에서 `처리 순서도`·`변경 이력`
   이 한 번 깨졌다(재실행 시 478건 통과). 원인 미확인.
